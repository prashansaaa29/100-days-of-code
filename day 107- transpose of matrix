class Solution:
    def transpose(self, matrix, n):
        arr = [[0]*n for _ in range(n)]
        for i in range(n):
            for j in range(n):
                arr[i][j]=matrix[j][i]
        for i in range(n):
            for j in range(n):
                matrix[i][j]=arr[i][j]
        # Write Your code here
