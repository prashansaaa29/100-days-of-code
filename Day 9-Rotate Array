class Solution:
    #Function to rotate an array by d elements in counter-clockwise direction. 
    def rotateArr(self,A,D,N):
        '''for i in range(D):
            A.append(A.pop(0))
        return A'''
        D = D%N
        A[:]=A[D:]+A[:D]
        return A
